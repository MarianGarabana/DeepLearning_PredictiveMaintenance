# Session Notes — June 16, 2026

## What We Did

### 1. Pulled Frontend Work from GitHub
- 9 commits were behind — pulled `frontend/setup` branch merge
- Frontend now has full Next.js app: pages, components, demo data, Tailwind, Docker

### 2. Fixed `/engine/{id}` Backend Endpoint
- Was hardcoded placeholder (always returned RUL 74, empty history)
- Now loads real sensor history + RUL history from demo JSON files
- Each engine maps to a scenario: healthy / degrading / critical
- `current_rul` stays consistent with fleet endpoint values

**Engine → Scenario mapping:**
| Engine | Scenario |
|--------|----------|
| ENG-001, ENG-002 | healthy |
| ENG-003, ENG-005, ENG-006 | degrading |
| ENG-004 | critical |

### 3. Fixed CORS
- Was hardcoded to `localhost:3000` only — would block Vercel frontend
- Now defaults to `*` (open for demo), overridable via `ALLOWED_ORIGINS` env var

### 4. Git LFS Migration
- HuggingFace rejected push due to binary files
- Migrated `.keras`, `.pkl`, `.png` to Git LFS across full history
- Force-pushed rewritten history to both `origin` (GitHub) and `hf` (HF Spaces)

### 5. Backend Deployed to HuggingFace Spaces ✅
- Space: https://huggingface.co/spaces/MarianGarabana/predictive-maintenance-api
- API URL: https://mariangarabana-predictive-maintenance-api.hf.space
- Verified live: model loaded, Uvicorn running on port 7860

### 6. Frontend Build Fixes (Local Build Passes ✅)
Multiple errors fixed for Vercel compatibility:

| Error | Fix |
|-------|-----|
| `TS5023: Unknown compiler option 'useDefineForModule'` | Replaced entire tsconfig with clean Next.js standard config |
| `Cannot find module '@tailwindcss/postcss'` | postcss.config.mjs: switched from Tailwind v4 plugin to v3 (`tailwindcss` + `autoprefixer`) |
| `Property 'toFixed' does not exist on type 'ValueType'` | `FeatureImportance.tsx`: guard with `typeof value === 'number'` |
| Conflicting `app/` directory (Geist font error) | Removed untracked `frontend/app/` scaffold |

### 7. Vercel Frontend Deploy — FAILED ❌
- Local build passes cleanly
- Vercel kept seeing old `useDefineForModule` despite correct file in git
- Root cause unclear — possible Vercel cache or deployment not picking up latest commit
- All fixes are committed and pushed to GitHub (`main` branch, commit `9bc2973`)

---

## Current State

| Component | Status |
|-----------|--------|
| NASA CMAPSS data | ✅ Downloaded |
| Model training (4 models) | ✅ Done — RMSE 13.57 (GRU), 14.52 (LSTM+Attention deployed) |
| Backend (FastAPI) | ✅ Live on HuggingFace Spaces |
| Frontend build | ✅ Passes locally |
| Frontend deploy (Vercel) | ❌ Failing — needs fix |
| README URLs | ⚠️ Backend URL updated, frontend URL still placeholder |
| Presentation deck | ❌ Not started |

---

## Remaining Tasks (Priority Order)

### Critical Before Presentation

1. **Fix Vercel deployment**
   - Try: Vercel dashboard → Settings → check Root Directory is set to `frontend`
   - Try: delete the Vercel project and re-import from GitHub fresh
   - Try: add env var `NODE_OPTIONS=--max-old-space-size=4096` in Vercel settings
   - Alternative: deploy frontend via `vercel` CLI from the `frontend/` directory
     ```bash
     cd frontend
     npx vercel --prod
     ```

2. **After Vercel URL obtained:**
   - Update `README.md` Live Demo table with Vercel URL
   - Add `ALLOWED_ORIGINS=https://YOUR_APP.vercel.app,http://localhost:3000` to HuggingFace Space secrets (Settings → Variables)
   - This locks down CORS for production

3. **Test end-to-end integration**
   - Open Vercel frontend → fleet page loads (calls `/fleet`)
   - Click engine → detail page loads (calls `/engine/{id}`)
   - WhatIfAnalyzer → runs prediction (calls `/predict`)
   - Simulation → calls `/simulate/start` + `/simulate/next/{id}`

4. **Presentation deck (PDF)**
   - Required deliverable — submit via campus platform
   - 15 min total: pitch + live demo + Q&A
   - All 4 members must speak
   - Suggested structure (suggested ~2 min each):
     1. Business problem & ROI (cost of unplanned engine failure = ~$500K–$2M per incident)
     2. Dataset & preprocessing (NASA CMAPSS, 17 features, sliding window)
     3. Model architecture & why LSTM (time-series, sequence modeling)
     4. Results (RMSE 14.52, 92% within ±25 cycles, comparison table)
     5. Live demo of MVP
     6. Q&A

### For Exceptional Grade (rubric 90-100%)
- Add quantified ROI to frontend or README:
  - Aviation MRO market: early warning = 30–40% reduction in unplanned maintenance cost
  - A single avoided AOG (Aircraft on Ground) event saves ~$50K–$150K/day
  - Model at 92% accuracy within ±25 cycles gives ~3-week advance warning

---

## Key URLs
- GitHub: https://github.com/MarianGarabana/DeepLearning_PredictiveMaintenance
- HF Space (backend): https://huggingface.co/spaces/MarianGarabana/predictive-maintenance-api
- API docs: https://mariangarabana-predictive-maintenance-api.hf.space/docs
- Frontend: ❌ not yet deployed
